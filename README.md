# Hot Reloading Test Runner

Requires pipenv.

Installation:

```
    python3 -m venv ./.venv
    pipenv install
```

Run

```
    pipenv run python3 .
```

or if in virtual env

```
    python3 .
```
